CREATE TABLE IF NOT EXISTS Shippers (
    ShipperID INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    CompanyName VARCHAR(40) NOT NULL CHECK (CompanyName <> ''),
    Phone VARCHAR(24) NOT NULL
);