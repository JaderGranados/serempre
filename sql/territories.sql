CREATE TABLE IF NOT EXISTS Territories (
    TerritoryID INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    TerritoryDescription VARCHAR(50) NOT NULL,
    RegionID INTEGER REFERENCES Region(RegionID)
);